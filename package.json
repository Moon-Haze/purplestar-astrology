{
  "name": "purplestar-astrology-skill",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "紫微斗数排盘与解读 skill 的运行时依赖。排盘内核是 ziwei-master 项目的私有源码（未发布到 npm），已直接置于 scripts/；本文件只声明内核所需的 npm 包。",
  "scripts": {
    "test": "node test/lib/run.ts",
    "test:corpus": "tsx test/tools/full-corpus.ts",
    "build:db": "tsx tools/db/build-duckdb.ts",
    "verify:db": "tsx tools/db/verify-duckdb.ts",
    "query": "tsx tools/db/query.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "iztro": "2.6.1",
    "lunar-typescript": "1.8.6"
  },
  "devDependencies": {
    "@duckdb/node-api": "1.5.5-r.5",
    "@types/node": "^26.6.2",
    "tsx": "^4.19.1",
    "typescript": "^7.0.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}