# ── 依赖 ──────────────────────────────────────────────────
# 注意：不能用 "skills/ziwei-doushu/node_modules/" 这类带前缀的写法，
# 依赖目录就在仓库根下的 node_modules/，带前缀会漏掉它。
node_modules/

# ── 工具 / 编辑器缓存 ─────────────────────────────────────
.codegraph/
.history/

# ── 本地配置 ──────────────────────────────────────────────
.claude/
.mcp.json

# ── 上游参照仓库（只读快照，不入版本控制）────────────────
reference/

# ── 系统垃圾 ──────────────────────────────────────────────
.DS_Store
*.log
npm-debug.log*
