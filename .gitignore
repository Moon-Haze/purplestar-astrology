# ── 依赖 ──────────────────────────────────────────────────
# 注意：不能用 "skills/purplestar-astrology/node_modules/" 这类带前缀的写法，
# 依赖目录就在仓库根下的 node_modules/，带前缀会漏掉它。
node_modules/

# ── 工具 / 编辑器缓存 ─────────────────────────────────────
.codegraph/
.history/
.vscode/

# ── 本地配置 ──────────────────────────────────────────────
# 放行 CLAUDE.md —— 它是项目的协作约定，不随仓库分发就毫无意义（他人克隆后
# 拿到的是写着已失效信息的旧版）。其余 .claude/ 内容（settings.json、本地
# 缓存等）仍不入版本控制。
.claude/*
!.claude/CLAUDE.md
.mcp.json

# ── 上游参照仓库（只读快照，不入版本控制）────────────────
# reference/ziwei-samples-toolkit/samples-out 是排盘基准的**原始语料**：
# 5.5 GB / 720 个 jsonl.gz / 60 个年份目录（1924–1983）。
# ⚠️ 它与 db/ziwei.duckdb 是同一份语料的两个载体，语义等价、互为验证 —— 不是备份关系。
# ⚠️ 核查本目录请用 find / stat / du，**不要用 ls**：本机 ls 是指向
#    `eza -al --git-ignore` 的别名，会把整个目录显示成空的
#    （2026-09-25 曾据此误判「语料已丢失」，见 docs/superpowers/specs/ 下的设计文档）。
reference/

# ── 样本数据集（随仓库分发，**不忽略**）────────────────────
# db/dataset/samples.parquet 与 db/dataset/palaces.parquet 是排盘基准工具
# （test/lib/sample-source.ts）的数据源，共 0.8 MiB + 12.0 MiB，克隆即可用、不必重建。
# 由 641.5 MiB 的 DuckDB 库导出而来 —— Parquet 对嵌套的星曜名列做字典编码，
# 同一份数据压到 1/50（DuckDB 的 LIST<VARCHAR> 是逐元素存放的）。
# 故此处**刻意不写**这两个文件。

# ── 不入版本控制的数据 ────────────────────────────────────
# db/dataset/topics-*.parquet：各主题解读文本的分片（11 片 / 379 MiB），体积过大。
# db/ziwei.duckdb：上面那份数据集的**导出来源**（641.5 MiB，超 GitHub 单文件 100 MiB 硬限），
#   属构建中间物 —— 数据集本身已入库，所以它随时可弃。同名的 .wal / .progress.json /
#   .dict.jsonl 都是它的边车，一并忽略。
# db/dataset.staging/：构建期的暂存分片与半截产物。
db/dataset/topics-*.parquet
db/dataset.staging/
db/ziwei.duckdb
db/ziwei.duckdb.*

# ── 系统垃圾 ──────────────────────────────────────────────
.DS_Store
*.log
npm-debug.log*
samples-out/**
