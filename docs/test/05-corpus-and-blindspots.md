# 05 · 数据集全量核验与盲区补测报告

**日期：** 2026-09-19
**被测对象：** 518,400 条全量语料（载体：`db/ziwei-s.duckdb`，等价于 `reference/ziwei-samples-toolkit`）+ `patterns.ts`（格局）+ `heming`（合盘）+ SKILL.md ↔ CLI 一致性
**状态：** ✅ **全量核验已完成**（518,400/518,400 零差异，2026-09-23 重启跑完，见第五节）；✅ 格局 / 合盘 / 一致性审计已完成；✅ 6 处文档漂移已修（2026-09-19）；✅ **P1 的处置方向已被后续决策反转**（2026-09-19，见文末「后续修正」）；✅ **格局识别已固化为层 3 的 50 项独立预言机**（2026-09-20，见文末「后续修正（二）」）；✅ **合盘已固化为 12 条独立预言机**（2026-09-20，见文末「后续修正（三）」）；✅ **流年/流月四化已固化为 9 条独立预言机**（2026-09-23，见文末「后续修正（四）」）；✅ **1984–2100 恒等式扫描与 taibu 对拍完成**（2026-09-23，见文末「后续修正（六）」）

---

## 一、执行方式

本次测试针对 [test/README.md](../../test/README.md) 自述的**六个盲区**中的后两个 —— 格局识别与合盘
「**仍是空白**」—— 以及数据集的全量核验。

| 项目        | 规模                                                                 |
| ----------- | -------------------------------------------------------------------- |
| 数据集      | `db/ziwei-s.duckdb`，0.67 GB / 518,400 条（原始载体 `reference/…/samples-out`，5.5 GB / 720 分片） |
| 语料条数    | 518,400（60 年 × 12 月 × 720 条/月；1924–1983）                      |
| 完整性      | ✅ 720/720 分片齐备，每片 720 条，抽样解压正常                        |
| 全量核验    | `node test/tools/full-corpus.ts`（单线程，实测约 16.3 ms/条）         |
| 格局 / 合盘 | 无外部基准，故**自建独立基准**（详见第三节，这是本次的主要方法产出） |

**基线（本次开跑前实测）：** `selftest` **44/44** ✅ · `npm test` **346/346** ✅（8.0 秒）

---

## 二、结果总览

| 维度                         | 结果                                                 |
| ---------------------------- | ---------------------------------------------------- |
| 数据集全量核验               | ✅ **完成：518,400/518,400 零差异**（见第五节收官结论） |
| 格局识别 · 安星规律等价关系  | ✅ **2880 张盘零不符**                                |
| 格局识别 · 性别无关性        | ✅ 48 对全部一致                                      |
| 格局识别 · 字段自洽性        | ✅ 0 异常（level / source / conditions 齐备且无矛盾） |
| 格局识别 · 覆盖率            | ✅ 59 个格局可命中；零格局盘仅 0.07%                  |
| 合盘 · 十二宫定位恒等式      | ✅ 72 盘 × 12 宫全部符合                              |
| 合盘 · 与 analyze 交叉一致   | ✅ 6/6 逐字段相同                                     |
| 合盘 · A/B 参数隔离          | ✅ 交换后双方盘精确互换                               |
| 合盘 · 性别护栏 / 晚子时提醒 | ✅ 三方（单人、a-、b-）文案均正确                     |
| **SKILL.md ↔ CLI 一致性**    | ❌ **6 处漂移**（其中 1 处会直接导致误用）            |

---

## 三、方法产出：如何给「零基准」的模块建立独立基准

这是本次测试最有价值的部分，也是 [docs/test/README.md](README.md) 那条教训的直接应用：

> **判断一个交叉验证有没有价值，先问：它和被测对象共享了多少计算路径。**

格局与合盘在数据集里**没有基准**（样本无 `patterns` 字段、样本是单人盘），而这两处的实现又
**无法用同源比对验证**——拿 `patterns.ts` 自己算的期望值去比对 `patterns.ts`，只是复读机。

本次改用**紫微斗数安星规律推导的确定性等价关系**。这类关系的正确性来自安星法本身，
与被测实现零共享路径：

| 等价关系                                                                 | 来源               |
| ------------------------------------------------------------------------ | ------------------ |
| 紫府同宫 ⟺ 紫微与天府同宫 ⟺ 紫微落在寅或申                               | 紫微/天府安星表    |
| 天府与天相恒相隔 4 宫                                                    | 十四主星排布恒等式 |
| 杀破狼 ⟺ 七杀/破军/贪狼三星齐入命宫三方四正                              | 格局定义           |
| 府相朝垣 ⟺ 天府与天相均在命宫三方四正且不同宫                            | 格局定义           |
| 十二宫相对命宫的偏移固定（命 兄 夫 子 财 疾 迁 友 官 田 福 父 = 0,−1,…） | 十二宫排列恒等式   |
| 格局识别结果与性别无关（性别只影响大限顺逆）                             | 安星法             |

> ⚠️ **这些关系的边界**：它们验证的是「识别逻辑与安星规律是否一致」，**不是**「格局的命理判词是否
> 正确」。判词取自古籍，属知识层，无外部可验证源 —— 和 04 号报告里 taibu 交叉验证的边界同构。

### 顺带验证到的结构概率

独立统计的命中率与理论值吻合，可作为旁证：

| 格局     | 实测命中率 | 理论值       | 说明                                     |
| -------- | ---------- | ------------ | ---------------------------------------- |
| 杀破狼   | 25.21%     | 3/12 = 25%   | 命宫落在杀破狼三宫的结构概率             |
| 府相朝垣 | 24.79%     | 3/12 = 25%   | 天府在命宫三合线上的概率                 |
| 紫府同宫 | 18.47%     | 2/12 ≈ 16.7% | 偏差来自采样网格的农历日分布，非实现问题 |

---

## 四、发现的问题

### 🟡 P1：宫名口径未文档化，「交友宫」查不到（会直接导致误用）

CLI 的十二宫用**「仆役」**，而紫微斗数日常更常说**「交友宫」**。SKILL.md 只写 `--focus <宫名>`，
举例恰好是「财帛」「官禄」这两个两家通用的名字，**从未列出过标准宫名**。

```bash
$ node scripts/purple-star.mjs analyze --date 1990-05-15 --time 09:30 --gender male --focus 交友
【聚焦失败】找不到宫位「交友」。可用：福德、田宅、官禄、仆役、迁移、疾厄、财帛、子女、夫妻、兄弟、命宫、父母
```

用户说「看看我的交友宫」时，模型很可能直接传 `--focus 交友` 而失败。
**失败提示本身是好的**（列出了全部可用宫名），属于可用性缺口而非逻辑缺陷。

同时实测到两条**未文档化的能力**：`--focus` 也接受**地支名**（`--focus 子` → 命宫），
以及 `--focus` 的匹配是 `p.name === focus || BRANCHES[p.branch] === focus`。

**建议**：SKILL.md 补一张十二宫标准名表，并注明可用地支。

### 🟡 P2：内核行数漂移

SKILL.md 称「**6613 行**自定义内核代码（`scripts/` 下 `.ts` 计，不含 `lunar-javascript.d.ts`）」，
实测 **6621 行**（`6648 − 27`），差 8 行。

`patterns.ts` 的「1190 行」则**准确**。说明只有总数这一处失准，属内核演化后未回填。

### 🟡 P3：`npm test` 耗时在 SKILL.md 与 test/README 中互相矛盾

| 位置                           | 声称    | 实测       |
| ------------------------------ | ------- | ---------- |
| `SKILL.md`（「若脚本报错」节） | 约 5 秒 | **8.0 秒** |
| `test/README.md` 第一节        | 约 8 秒 | 8.0 秒 ✅   |

两处**自相矛盾**，SKILL.md 的那个是陈旧值。（8.0 秒为无后台负载时的干净测量。）

### 🟢 P4：`selftest` 首行内容描述失准

SKILL.md 称「`selftest` **首行**会打印当前生效的内核根」。实际首行是自检标题，
**第二行**才是内核根：

```text
紫微斗数 skill 回归自检 —— 通过 44/44        ← 首行
内核根：/home/swix/Code/JSProjects/purplestar-astrology/scripts   ← 第二行
```

### 🟢 P5：目录结构图缺 `test/`、`docs/`

SKILL.md 的目录结构图只列了 `SKILL.md / README.md / LICENSE / package.json /
package-lock.json / scripts/ / node_modules/`，但正文明确引导读者去跑 `npm test`
并阅读 `test/README.md` —— 图与正文对不上。根目录另有未列出的 `tsconfig.json`
与 `docs/`。

> 注：`CLAUDE.md` 称本项目「**也不需要 `tsconfig.json`**」（指运行不依赖它），
> 但该文件确实存在且含 `paths: {"@/*": ["./scripts/*"]}`。两者不冲突
> —— 运行时不依赖、但 IDE/`tsc` 会用 —— 只是措辞容易被读成「不存在」。

### 🟢 P6：`brightness` 一节的字段名用的是上游 iztro 的

SKILL.md 称「`minorStars` / `adjectiveStars` 不带该字段」。本项目 `chart.palaces[].stars[]`
是**扁平数组 + `type` 字段**（`major` / `minor` / `sha` / `lucky`），并不存在
`minorStars` / `adjectiveStars` 这两个键。核心结论正确（实测：主星 14/14 带 `brightness`，
其余三类 0 颗带；取值仅 `bright` / `dim` / `normal` 三级），但字段名会让读者去找不存在的键。

---

## 五、数据集全量核验（✅ 已完成）

首次跑约 1/20 时随会话中断丢失，2026-09-23 重启跑完：

| 项         | 值                                             |
| ---------- | ---------------------------------------------- |
| 状态       | ✅ **完成（2026-09-23 重启，2026-09-24 收官）** |
| 检查条数   | **518,400 / 518,400**（1924-01 ~ 1983-12，720 分片） |
| 完全一致   | **518,400**                                     |
| 有差异     | **0**                                           |
| 白名单放行 | 63,124 处（全部为已知的太阳/太阴@酉宫亮度）      |
| 实测速率   | 15.9 ms/条（共 8,257 秒 ≈ 2.3 小时）            |

**单分片实弹**（先行验证管线，`--year 1960 --month 6`）：720 条**完全一致**，
白名单放行 84 处（均为已知的太阳/太阴在酉宫亮度，见 `test/lib/compare.mjs`）。

**收官结论**：

- 白名单之外**零差异** —— 抽样 300 条能代表的，全量同样成立；抽样盖不到的角落
  （罕见的农历边界组合、极低频格局触发）也没有潜藏分歧；
- 全量跑在「生年四化改取农历年干」修复**之后**：层 1 严格比对 `siHua` 字段，
  518,400 条含全部「公历年 ≠ 农历年」出生日（每年 1/1 至春节前，约 15% 日历段）——
  相当于对修复后的年干口径做了一次**全谱验证**（300 条抽样中该日期段命中有限）；
- 白名单命中 63,124 处 ≈ 12.2%，全部是同一已知成因（iztro 2.5.8→2.6.1 的两星亮度），
  无新增成因 —— 比对器活着，且只在已知处放行。

---

## 六、本次测试未覆盖的

| 未覆盖项                     | 原因                                                        |
| ---------------------------- | ----------------------------------------------------------- |
| 格局判词的命理正确性         | 无外部可验证源（古籍判词属知识层）—— 已验的是识别逻辑       |
| 合盘结论的命理正确性         | 同上；已验的是结构、交叉一致性与护栏                        |
| 全量语料的**排盘正确性**     | 基准与内核同源 iztro，只能证「行为未变」，不能证「排得对」  |
| 真太阳时 / 晚子时 / 农历入参 | 本就不在数据集内（样本 `longitude` 恒 120、`hour` 仅 0–11） |

---

## 七、待办

- [x] P1 补十二宫标准名表（并注明 `--focus` 可用地支）—— **已修**（2026-09-19）
- [x] P2 / P3 改为量级表述 —— **已修**（2026-09-19）
- [x] P4 `selftest` 首行 → 第二行；P5 目录结构图补 `test/` / `docs/` / `reference/` / `tsconfig.json` —— **已修**（2026-09-19）
- [x] P6 把 `minorStars` / `adjectiveStars` 改为本项目实际的 `stars[].type` 口径 —— **已修**（2026-09-19）
- [x] 全量核验跑完，补录第五节结论 —— **已完成**（2026-09-23 重启跑完，518,400/518,400 零差异）
- [x] 格局的独立等价关系**固化为层 3 测试** —— **已完成**（2026-09-20，见文末「后续修正（二）」）
- [x] 合盘的独立等价关系**固化为层 2 / 层 3 测试** —— **已完成**（2026-09-20，见文末「后续修正（三）」）

### 修复记录（2026-09-19）

六处漂移全部落在 `SKILL.md` 单文件内，改动后复验：

| 处  | 改动                                                                                | 复验方式                                                             |
| --- | ----------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| P1  | `--focus` 一节补十二宫标准名表，点明第 8 宫是**仆役**，并注明可用地支名             | `--focus 仆役` ✅ / `--focus 交友` ❌ / `--focus 子` ✅                 |
| P2  | 「6613 行」→「约 6,600 行」；`patterns.ts`「1190 行」→「约 1,200 行」               | 附现场数行数命令，实跑得 6621 / 1190                                 |
| P3  | 「约 5 秒」→「数秒量级，视机器负载而定」                                            | 实跑 8.1 秒（且此时后台核验仍在跑）                                  |
| P4  | 「首行」→「标题行之下的第二行」                                                     | 跑 `selftest` 取前两行，实测吻合                                     |
| P5  | 目录图补 `test/` / `docs/` / `reference/` / `tsconfig.json`，并注明哪些是开发期资产 | 与 `find . -maxdepth 2` 对账                                         |
| P6  | `minorStars` / `adjectiveStars` → `stars[].type`（`major`/`minor`/`sha`/`lucky`）   | 实测：major 14/14 带 `brightness`，其余三类 0 颗带；两个旧键均不存在 |

**P2 / P3 刻意不给精确值**——本仓库的教训是精确数字必然漂移（它们已经漂过一次）。
改成量级表述后，这类改动不再产生新的文档债。

---

### 后续修正（2026-09-19）：P1 的处置方向整个反了

上面 P1 的修复是**按错误的前提**做的：它把「仆役」当成项目的既定口径，于是让文档去迁就实现
（SKILL.md 补表 + 点明「第 8 宫是仆役、传交友会失败」）。

但复核发现**「仆役」根本不是本项目的口径，而是上游技术栈的口径泄漏**：

| 来源                                                        | 第 8 宫叫法 |
| ----------------------------------------------------------- | ----------- |
| iztro（排盘引擎）                                           | 仆役        |
| `reference/ziwei-samples-toolkit`（数据集）                 | 仆役        |
| `test/fixtures/charts.jsonl`（本项目基准快照）              | 仆役        |
| **`scripts/nihai/tianji.ts`（倪海夏《天纪》原文）**         | **交友**    |
| **`scripts/ziwei/constants.ts` 的 `PALACE_NAMES_ORDER`**    | **交友宫**  |

`algorithm.ts` 原文是 `name: p.name`——宫名**直通 iztro，内核从未做过映射**，
于是项目自己写好的十二宫名表成了**死代码**（全仓零 import）。

**已按「实现迁就项目口径」重做**：内核加 `IZTRO_TO_PROJECT_PALACE` 映射，
十二宫统一带「宫」字，第 8 宫改名**交友宫**；旧写法（仆役 / 交友 / 去「宫」字简称）
全部保留为 `--focus` 的别名。所以上表 P1 那一行的复验结论
（`--focus 交友` ❌）**已经失效，现在四种写法都 ✅**。

连带修掉三处会因此**静默失效或崩溃**的硬编码（都是这次才暴露的）：

- `purple-star.mjs` 的 `heming`：取宫用旧名 → 文本路径 TypeError 崩溃、
  `--json` 路径更早 `return` 故**静默输出空数组**（比崩溃更糟）
- `patterns.ts` 的「化禄入财」「化权入官」：取宫用旧名 + 有 `if (!c)` 守卫
  → 两个格局**永不触发**，不报错
- `test/lib/compare.mjs`：基准样本存的是 iztro 名，故在比对器里做一次**单向**宫名翻译

后两处的守卫是**本次新增**的（此前 `--focus` 与 `heming` 全仓零覆盖）：

| 新增断言                                            | 层  | 反向实验验证                                                       |
| --------------------------------------------------- | --- | ------------------------------------------------------------------ |
| 十二宫映射表与 `PALACE_NAMES_ORDER` 同集合          | 3   | 把「仆役」映射成「官禄宫」→ 红                                     |
| iztro 直连词法（不读映射表）                        | 3   | 同上 → 红                                                          |
| 十二宫偏移算术 `(命宫 − 本宫 + 12) % 12`            | 3   | 同上 → 红                                                          |
| 化禄入财 / 化权入官（按偏移算术核对，双向）         | 3   | 退回旧宫名 → 仅这两条红，层 1 的 304 条照旧全绿                    |
| `--focus` 四种写法聚焦到同一宫                      | 2   | 删掉一个别名 → 红                                                  |
| `heming --json` 派生字段与 chart 自洽               | 2   | 查找键退回旧名 → 仅这两条红                                        |

> **这次最有价值的一点**：把映射表故意改错，层 1 的 300 条基准**照旧全绿**——
> 因为比对器与内核共用同一张表，会一起错。这是 [README](README.md) 那条贯穿性教训
> （02 号：比对器与内核同源同错）的**又一次实证**，见
> [test/README.md 第二节第 6 条](../../test/README.md)。

改动后 `selftest` **44/44** ✅、`npm test` **346/346** ✅（8.1 秒）—— 本次纯文档改动，内核未动。

---

### 后续修正（二）（2026-09-20）：格局识别补测 —— 70 个格局名的独立预言机

**日期：** 2026-09-20
**被测对象：** `scripts/ziwei/patterns.ts`（1190 行 / 41 个 detect 函数 / **70 个格局名**）
**状态：** ✅ 已完成并固化进层 3

#### 为什么补

[test/README.md](../../test/README.md) 自述的六个盲区里，「格局识别」是**最大的一个**：
`patterns.ts` 是全仓行数第二多的文件，而它此前**没有任何基准** —— 只有第三节那次留下的
「化禄入财 / 化权入官」两条守卫，其余 40 余个格局零覆盖。

本次把 2026-09-19 那次建立的**安星法偏移算术**方法推广成一套覆盖**全部 70 个格局名**的
独立预言机，并按本项目的规矩做了**反向验证**（不反向验证过的断言不算数）。

#### 独立性从哪来：定位路径分岔

预言机若照抄实现的判定逻辑，就只是复读机。这里的分岔点在**「怎么从 chart 找到那几个宫」**：

| | 实现 `patterns.ts` | 预言机（层 3） |
| --- | --- | --- |
| 三方四正 | 地支算术 `[m, (m+4)%12, (m+8)%12, (m+6)%12]` | 宫名 `find(p => p.name === "财帛宫")` |
| 夹宫 | 地支算术 `(branch + 11) % 12` / `(branch + 1) % 12` | 宫名「兄弟宫 / 父母宫」 |
| 火贪 / 铃贪 | 正向枚举 `(tan.branch + 4/6/8) % 12` | 相对偏移取模 `(sha.branch − tan.branch + 12) % 12` |

宫名本身的正确性由层 3 **既有**的两条偏移恒等式独立保证（本次未新增，见
[test/README.md 第二节第 6 条](../../test/README.md)），所以这不是「用一个没验过的量去验另一个量」。

共 **50 项断言**：44 条单格局预言机 + 3 条派生名（按星名展开的 `*化禄入命` 等）+ 3 条输出结构。

#### 反向验证：6 种注入，各自的爆炸半径

每条断言都做过分注入验证 —— 注入后**只有目标断言变红，无误伤**：

| 注入点 | 变红项 | 说明 |
| --- | --- | --- |
| 三方四正 `(m+4)` → `(m+5)` | **14 项** | 杀破狼、机月同梁、三奇加会、双禄朝垣、廉杀羊、巨火羊…… |
| 夹宫 `(branch+11)` → `(branch+1)` | **6 项** | 日月 / 辅弼 / 昌曲 / 魁钺 / 空劫夹命、羊陀夹忌 |
| 同宫判定 `!==` → `===` | 1 项 | 廉贞天相格 |
| 宫名字面量 `"财帛宫"` → `"官禄宫"` | **2 项** | 偏移算术块与新预言机块**同时**红 —— 两条独立路径互相印证 |
| 派生名 `"禄"` → `"权"` | 1 项 | `*化禄入命` |
| 回退 palaces 修复 | 1 项 | 输出结构断言 |

> ⚠️ **夹宫注入里「火铃夹命」没红** —— 它在 300 条基准里触发 **0** 次，断言是**空转**的。
> 这不是漏网，正是下面 `NO_HIT_IN_FIXTURES` 登记机制存在的理由。

#### 抓到的真实缺陷：「杀破狼」的 `palaces` 恒为空

新加的「每条 Pattern 的字段完整且取值合法」断言在 `1924-10-22 / 9 时 / female` 上炸出：

```text
1924-10-22 时9 female 的「杀破狼」：palaces 为空
```

**实测规模**：杀破狼的 86 次触发里，**55 次（64%）`palaces` 是空数组** —— CLI 会渲染出
「涉及宫位：」后面空无一物。这是**用户可见的输出缺陷**，不是内部字段问题。

**根因**：该字段用 `getMajorStarNames(p)[0]` 取**第一颗**主星来比对，而紫破 / 廉贪 / 武杀
这类**双主星同宫**极常见：

| 宫 | 主星数组 | `[0]` 取到 | 是否在 `has` 里 |
| --- | --- | --- | --- |
| 官禄 | `[廉贞, 贪狼]` | 廉贞 | ❌ |
| 财帛 | `[武曲, 七杀]` | 武曲 | ❌ |
| 命宫 | `[紫微, 破军]` | 紫微 | ❌ |

于是「七杀 / 破军 / 贪狼」一个都匹配不上，整张盘的宫位列表被清空。

**修复**：改为 `.filter(p => has.some(s => getMajorStarNames(p).includes(s)))` ——
与同文件 `detectJiYueTongLiang` 的**既有正确写法**对齐。只改**展示字段**，判定逻辑未动。

#### 效力边界（别高估）

- 只核对「**格局是否触发**」。`level` 分级、`description` 判词、`conditions` 文案**不覆盖**
- 预言机复刻的是**实现当前的口径**，不是照命理理想口径重写（见下条争议）
- 「昌曲夹命」「火铃夹命」在 300 条基准里触发 **0** 次 → 断言空转，已用
  `NO_HIT_IN_FIXTURES` **显式登记**（该集合里的格局若变成有命中，测试会失败并提示移除），
  这是「宁可显式失败，也不静默通过」的直接体现
- 另加一条**名册守卫**：输出里出现预言机表以外的格局名即失败，逼新增格局同步补预言机

#### 未决：火贪 / 铃贪的口径争议

`detectHuoTan` / `detectLingTan` 的命中条件实测为：

```text
贪狼与火（铃）同宫或会照  且  贪狼在命宫三方四正
```

**32 次命中里有 12 次，那颗煞星照不到命宫**。数学根因是一个容易想当然的点：

> **三方四正不是传递关系。**
>
> 命宫 `m` 的三方是 `{m, m+4, m+6, m+8}`。贪狼若在 `m+4`，**贪狼自己的**三方是
> `{m+4, m+8, m+10, m}` —— 与命宫三方差一个 `m+2`（对宫）。只有贪狼恰在命宫时两者才相同。
>
> 所以「贪狼会照火星」**不等于**「火星在命宫三方」。

实现 `conditions.required` 里明写的是两条：**「贪狼会照火铃」+「贪狼会照命宫三方」** ——
**代码与文档自洽**，故本次判定为**口径选择**而非 bug，预言机按实现复刻，并在测试代码注释里
显式记录了这一争议（含收紧方式：加一条 `SANFANG_NAMES.includes(sha.name)` 即可）。
**是否收紧留给项目方决定。**

#### 基线

| 项 | 改前 | 改后 |
| --- | --- | --- |
| `npm test` | 356 / 356 | **406 / 406** |
| 其中层 3 | 24 项 | **74 项**（+50） |
| `selftest` | 44 / 44 | 44 / 44（内核判定逻辑未动） |

内核侧改动仅 1 处（`detectShaPoLang` 的 `palaces` 字段），其余全部落在测试与文档。

---

### 后续修正（三）（2026-09-20）：合盘独立预言机 —— 填掉最后一个真空白

**日期：** 2026-09-20
**被测对象：** `cli/commands.ts` 的 `cmdHeming` + `cli/render.ts` 的 `mustPalace` / `locateSihua`
**状态：** ✅ 已完成并固化进层 2 与层 3

#### 为什么补

到（二）为止，[test/README.md](../../test/README.md) 自述的盲区里只剩**合盘**没填：层 2 原有的
两条断言只守「`heming` 跑得通」与「`--json` 的派生字段与 chart 自洽」—— 那守的是**宫名查找
落空**（旧名会让 `--json` 静默输出空数组、文本路径抛 TypeError），**不是合盘的论断内容**。

爆炸半径查询确认了这一点：`cmdHeming` / `mustPalace` / `locateSihua` 三处均显示
`no tests found within 3 caller hops`。而合盘出错**不报错**，只是输出一份看起来正常的错结论
—— 参数串台、取错宫、四化算错、交叉方向写反，都会安静地给出一个煞有介事的答案。

#### 独立性从哪来：期望值与被测值走两条路

沿用（二）的思路，但分岔点换了一处 —— 这次是**「期望值由谁算」**：

| | 被测值 | 期望值 |
| --- | --- | --- |
| 来源 | CLI 子进程的文本 / `--json` 输出 | 测试进程内 `generateChart` 独立排盘 |
| 复算方式 | — | 安星法偏移恒等式 + 四化表查表 |
| 另一条交叉路径 | — | **交换 `--a-*` / `--b-*` 后的另一次运行** |

关键约束是**单向**：只从 CLI 输出取值，从不反过来拿它算期望值。交换参数那次运行之所以算
「另一条路径」，是因为它经过的是同一段代码的**另一次执行**，若甲乙两侧被写了同一份信息，
两次运行的对称性会立刻破裂 —— 这是复制粘贴型缺陷的典型形态。

层 2 的 7 条 + 层 3 的 5 条，共 **12 条**：

| # | 断言 | 层 | 期望值来源 |
| --- | --- | --- | --- |
| 1 | 交换 `--a-*` / `--b-*` 后两方命盘精确互换 | 2 | 交换后的另一次运行 |
| 2 | 两方盘各自等于按同一出生信息独立排的盘 | 2 | `generateChart` + `asJson` 对齐 |
| 3 | 文本中命/夫/福三宫地支满足「夫 = 命−2、福 = 命+2」 | 2 | 安星法偏移恒等式（两个样本各验一遍） |
| 4 | 生年四化入夫妻宫的判定 | 2 | `getSiHuaByStem` 查表 + 直接定位星名 |
| 5 | 天作之合的交叉集合与判定方向 | 2 | 双向交集独立复算 |
| 6 | 晚子时提醒只落在命中的一方，且参数前缀正确 | 2 | 交换样本对照 |
| 7 | 夫妻宫空宫时断语改用借自对宫的主星 | 2 | `borrowedFromName` / `borrowedStars` |
| 8 | 12 个项目口径宫名都能取到且落在偏移恒等式要求的位置 | 3 | 偏移算术（300 盘 × 12 宫） |
| 9 | iztro 旧口径与去宫字简写一律抛错（不静默退化） | 3 | `mustPalace` 的契约 |
| 10 | 四化表涉及的星名都能在真实盘上找到 | 3 | 知识源完整性 |
| 11 | 四化只落在主星与文昌文曲左辅右弼上 | 3 | 星曜类型约束 |
| 12 | `locateSihua` 的落宫 = 按四化星名直接定位（逐盘） | 3 | 另一条定位路径 |

#### 反向验证：10 种注入，0 漏网

（二）的规矩是「不反向验证过的断言不算数」。本轮逐个注入失效模式，看**只有目标断言变红**：

| 注入 | 变红项 |
| --- | --- |
| 乙方出生信息改读 `--a-*` 前缀 | 6 |
| 夫妻宫取成福德宫 | 8 |
| 文本夫妻宫行渲染成福德宫地支 | 3 |
| 四化落宫比成福德宫 | 3 |
| 天作之合的交叉判定方向写反 | 3 |
| 晚子时甲 / 乙标签互换 | 3 |
| 空宫不再借对宫主星 | 3 |
| 四化表「廉贞」写成「廉真」 | 4 |
| 四化表混进杂曜「红鸾」 | 3 |
| `mustPalace` 从精确匹配退化为子串匹配 | 3 |

> ⚠️ **其中一条最初是漏网的。**「天作之合的交叉判定方向写反」（`fA ∩ mB` 写成 `fA ∩ mA`）
> 在**第一版样本上不会让任何断言变红** —— 那对样本两向交集皆空，写反与写对输出完全相同。
> 枚举换到「`甲夫 ∩ 乙命` 非空、而 `甲夫 ∩ 甲命` 为空」的一对之后才抓住，
> 此时判定从「单向对应」翻转为「无主星对应」。
>
> 这正是**样本守卫**存在的理由：每组样本都带一条「命中数 > 0，否则断言在空转」的断言。
> 与（二）里 `NO_HIT_IN_FIXTURES` 是同一个机制的两个形态 —— **宁可显式失败，也不静默通过**。

#### 效力边界（别高估）

- 断语**文案**（夫妻宫断语的吉象 / 凶象 / 婚期段落、`HEMING_METHODOLOGY` 正文）**不覆盖**，
  只覆盖「取用了哪颗星的断语」
- 预言机复刻的是**实现当前的口径**，不是照命理理想口径重写
- 样本是**挑过的**（为空宫借宫、交叉方向、四化命中各挑了专用样本），不是随机抽样
- 合盘仍**没有外部基准** —— 与（二）同理，验的是「实现与安星规律是否一致」，
  不是「合盘结论的命理正确性」

#### 基线

| 项 | 改前 | 改后 |
| --- | --- | --- |
| `npm test` | 406 / 406 | **418 / 418** |
| 其中层 2 | 22 项 | **29 项**（+7） |
| 其中层 3 | 74 项 | **79 项**（+5） |
| `selftest` | 44 / 44 | 44 / 44（内核未动） |
| `npm run typecheck` | 0 错误 | 0 错误 |

内核侧**零改动** —— 全部落在 `test/cli.test.mjs` 与 `test/invariants.test.mjs`
（变异测试注入的改动已全部恢复，`git status` 可复核）。

---

### 后续修正（四）（2026-09-23）：流年/流月四化独立预言机

**日期：** 2026-09-23
**被测对象：** `sihua.ts` 的 `getLiuNianSiHua` / `getLiuYueStemIndex` / `getLiuYueSiHua` + `commands.ts` 的组装
**状态：** ✅ 已完成并固化进层 2 与层 3

#### 为什么补

（二）（三）填掉格局与合盘后，三合派三层四化里的两个**动态层**仍只有一条边角断言
（`--liunian` 非数字报错）：流年（公历年 → 年干）与流月（五虎遁）的**算术**零覆盖。
`getLiuYueStemIndex` 的爆炸半径查询显示 `no tests found within 3 caller hops` ——
与格局、合盘补测前同一形状。

#### 独立性：三条互不相干的路径

| 被测 | 独立路径 |
| --- | --- |
| 五虎遁月干 | **口诀独立表**：甲己之年丙作首……120 格逐格写死，与实现的 `startStemOfYin` 零共享代码 |
| 流年年干 | **lunar-typescript 年柱**：1924–2100 逐年比对。取公历 6 月 15 日取年柱 —— 远离立春（约 2 月）与正月初一（1–2 月）两个切年边界，年干无歧义（lunar 年柱以立春切年、紫微以正月初一切年，年中日期两种切法等价） |
| 流年年干 | **万年历固定向量**：1900 庚 / 1984 甲 / 1996 丙 / 2000 庚 / 2024 甲 / 2026 丙，纸面常识写死 —— 防 lunar 与实现**同错**（都是代码，第三条路径是纸面） |

层 2 另钉三条**只有端到端才能钉住的口径**：

1. **流月由流年干推、不串生年干**：生年庚 × 流年甲 → 正月月干**丙**（串成生年干庚会得
   戊）—— 与（三）「参数串台」同一形态，专挑生年干 ≠ 流年干的样本；
2. **生年与流年的年干口径刻意分家**：1990-01-15 出生者的【生年四化】年干**己**（农历己巳
   年腊月）与【1990 流年四化】年干**庚**（公历取模）同屏 —— 钉住「生年四化改取农历
   年干」那次修复的行为边界（生年走农历、流年刻意留在公历）；
3. `--json` 的 `located` 落宫与本进程**按星名直接找宫**逐项一致（不走 `locateSihua`
   的渲染路径）。

#### 反向验证：4 种注入，0 漏网

| 注入 | 变红项 |
| --- | --- |
| 五虎遁表改一格（甲年起戊） | 6 |
| 流年取模偏移一位（`(year-4)` → `(year-3)`） | 11 |
| 流月改用生年干推（串台） | 5 |
| 月序偏移一位（`(month-1)` → `month`） | 8 |

#### 基线

| 项 | 改前 | 改后 |
| --- | --- | --- |
| `npm test` | 422 / 422 | **431 / 431** |
| 其中层 2 | 33 项 | **39 项**（+6） |
| 其中层 3 | 79 项 | **82 项**（+3） |
| `selftest` | 46 / 46 | 46 / 46（内核未动） |
| `npm run typecheck` | 0 错误 | 0 错误 |

内核侧**零改动** —— 全部落在 `test/cli.test.ts` 与 `test/invariants.test.ts`。

---

### 后续修正（五）（2026-09-23）：全量核验重启

第五节那次全量核验（约 3 小时）随会话中断而丢失，本日重启：

- 数据集本体位于上游项目 `~/Code/JSProjects/ziwei-samples-toolkit/samples-out`（5.5 GB，
  60 个年份目录 / 720 个 `jsonl.gz`），本仓 `reference/` 下的这份是**实体目录**，
  **不是符号链接**（实测 `[ -L ]` 逐层验过，`reference/`、`reference/ziwei-samples-toolkit`、
  `.../samples-out` 三层全为实体目录）；
- 2026-09-25 补：同一份语料已等价载入 `db/ziwei.duckdb`（1.7 GB，`samples` + `palaces` 两表），
  两个载体**并存、互为验证**。基准工具（`full-corpus.ts` / `build-fixtures.ts`）的数据源已改为
  DuckDB，jsonl 语料转为**验收期参照物**（`verify-source.ts` 逐条逐字节互验）；
- ⚠️ 核查这两个目录请用 `find` / `stat` / `du`，**不要用 `ls`**：本机 `ls` 是指向
  `eza -al --git-ignore` 的别名，会把 `.gitignore` 覆盖的 `reference/`、`db/` 显示成空目录。
  2026-09-25 曾据此误判「语料已丢失」，并把错误结论写进了设计文档与提交 `461cead`；
- 冒烟：1960-06 单分片 720 条完全一致、白名单放行 84 处（与首次核验基线一致）、
  16.7 ms/条 —— 全量 518,400 条预计约 2.4 小时；
- **本次新增价值**：层 1 严格比对 `siHua` 字段，「生年四化改取农历年干」修复之后，
  全量能把 300 条抽样覆盖不足的「公历年 ≠ 农历年」出生日段（每年 1/1 至春节前，
  约占日历 15%）全部压上 —— 相当于对修复后的年干口径做一次全谱验证。

结果已补录第五节（518,400/518,400 零差异）。

---

### 后续修正（六）（2026-09-23）：年份段恒等式扫描 + taibu 第三方对拍

**日期：** 2026-09-23（2026-09-24 收官落盘）
**被测对象：** 1984–2100 无基准年份段的排盘自洽性 + 农历换算/年柱口径的第三方互证
**状态：** ✅ 已完成（一次性扫描 + 对拍，工具固化在 `test/tools/year-scan.ts`）

#### 一、年份段恒等式扫描（新工具 `year-scan.ts`）

**为什么**：数据集止于 1983，而**实际来排盘的用户几乎都生于 1984 之后**——这 116 年
零外部基准，是结构性缺口。造不了基准就压**与年份无关的恒等式**。

**样本**：1900–2100 全域 × 每月 3 日 × 12 时辰 × 性别交替 = **86,832 条**（15.8 ms/条，23 分钟）。

**压的恒等式**（口径照层 3 复刻）：十二宫偏移、宫名集合、十四主星各一、生年四化与
农历年干四化表逐颗一致、大限 12 步区间连续宫名对齐、空宫借宫、**农历往返**
（公历 → lunarInfo → lunar-typescript 回公历，闰月用负月序）、年柱双向核对。

**结果：零违例。** 另有两个过程性收获：

1. **首版脚本误报了一片「年柱分叉」** —— 根因是检查器自己拿「年中日期取的年柱」去比
   「出生日的年柱」，两个根本不是同一天（探针口径错 ≠ 被测对象错，第 N 次实证）；
2. **实测纠正了一个想当然**：lunar-typescript 的 `getYearInGanZhi()` 按**正月初一**切年
   （与本项目同口径），立春界是 `getYearInGanZhiByLiChun()`（按日）/ `getYearInGanZhiExact()`
   （按时刻）—— 三个 API 的口径已写进脚本注释与层 3 注释。

**效力边界**：自洽性 ≠ 正确性 —— 抓「排出结构上不可能的盘」，不抓「结构合法但安错星」。

#### 二、taibu 第三方对拍（12 条年柱 + 1 条全盘）

排盘层与本项目疑似同源 iztro（04 号已判），故本次对拍主打**非同源**部分：

- **年柱 × 12**：样本专挑切年边界（春节～立春窗口 5 条 + 立春当日 1 条 + 一致区 6 条 +
  支持域上下沿）。**12/12 精确归因，零真实分歧**：taibu 年柱 = 立春**精确时刻**切年
  （1975-02-04 立春 18:59，样本取 12:00 未过立春 → 甲寅，与 `getYearInGanZhiExact()` 一致），
  本项目 = 正月初一切年，两口径的固有分叉全部精确落在预期窗口内 —— 分叉的重合本身
  就是农历换算正确性的第三方证明（本机 lunar-typescript 与远端 taibu 在分钟级边界逐字一致）；
- **紫微全盘 × 1**（1990-05-15 09:30 lng116.4）：农历/五行局/命宫/身宫/紫微位/大限首段/
  **生年四化逐宫落位** —— 7/7 逐字段一致。四化落宫维度是 04 号对拍没有的，正好第三方
  佐证「生年四化改取农历年干」修复。

明细见 `log/2026-09-23-taibu-crossval.log`。

---

### 后续修正（七）（2026-09-26）：语料载体换代 —— `ziwei-s.duckdb`

**日期：** 2026-09-26
**被测对象：** 基准语料的 DuckDB 载体本身（换代后的 schema 兼容性与端到端可用性）
**状态：** ✅ 已完成（首末年补证零差异；读取代码未改一行）

（五）里记的 `db/ziwei.duckdb`（1.76 GB）已被 `db/ziwei-s.duckdb` 取代，旧文件已从 `db/` 移除。
换代动机与体积账见 `docs/doubu-analysis/02-duckdb-compression.md`。

**新库实际形态**（实测，非推断）：

| 表        | 行数      | 说明                                                      |
| --------- | --------- | --------------------------------------------------------- |
| `samples` | 518,400   | 28 列；比旧库多 8 个 `sihua_*_star` / `sihua_*_palace` 列 |
| `palaces` | 6,220,800 | 16 列；比旧库多 `sihua_flags`                             |
| `topics`  | 518,400   | 14 列论断文本 —— **保留在主库内**                         |

实测体积 **672,935,936 字节（0.67 GB）**。⚠️ 这与 `02-duckdb-compression.md` 方案 1 设想的
「重建时只导 `samples` + `palaces`、估 150–300 MB」不同：实际剔除的是 `topic_dict` 与
`topic_lines` 两张，`topics` 表**仍在库内** —— 这正是停在 0.67 GB 而非估算区间的原因。
另有 `db/topics/` 下 502 MB parquet，是 topics 数据移出主库的产物。

**schema 兼容性：纯增量，读取代码零改动。** 列类型仍为
SMALLINT / TINYINT / VARCHAR / BOOLEAN / VARCHAR[] / DOUBLE，**没有 ENUM 化、没有重排序**，
`COLUMNS` 白名单里那 33 列逐列存在。多出的列本就不在名单内 —— 该模块自建立起就显式列名、
不 `SELECT *`，理由写在 `COLUMNS` 旁的注释里，这次换代只是又一次印证了那个决定。

**端到端验证**（`node test/tools/full-corpus.ts --year <Y>`，取语料的首年与末年）：

| 年份 | 条数  | 完全一致 | 有差异 | 白名单放行 | 耗时                 |
| ---- | ----- | -------- | ------ | ---------- | -------------------- |
| 1924 | 8,640 | 8,640    | 0      | 1,060      | 140.1 秒（16.2 ms/条） |
| 1983 | 8,640 | 8,640    | 0      | 1,060      | 142.0 秒（16.4 ms/条） |

两年退出码均为 0。

**顺带修掉一处真实痛点：库被别的进程锁住。** DuckDB 的读写锁与只读锁互斥，而编辑器里的
DuckDB / SQL 扩展打开该库时会以读写模式持有它 —— 基准工具当场报
`IO Error: Could not set lock on file`。裸 IO 错误只说明「有冲突」，不指向解法，而最贵的
误判是去**找根本不存在的备份**。`test/lib/sample-source.ts` 因此新增 `lockedDbHint`：
把「只是锁冲突，不是文件损坏、也不是语料丢失」写在最前，给出 `lsof <库>` 的定位法与
「关掉占用它的进程」的解法，并说明 `npm test` 不受影响（日常回归不读这个库）。
该指引在真实锁冲突下验证通过（占用者正是编辑器进程），层 5 已补断言锁住其文本。

**日常回归不受影响**：`npm test` 472 项全过（304 / 65 / 82 / 6 / 15），`npm run typecheck`
0 错误 —— `npm test` 不读这个库，fixtures 已入库。
