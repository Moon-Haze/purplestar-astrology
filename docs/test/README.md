# 测试报告存档

本目录存放**历次测试的报告**。与 [test/README.md](../../test/README.md) 分工不同：

|      | 本目录 `docs/test/`                                | [test/README.md](../../test/README.md)       |
| ---- | -------------------------------------------------- | -------------------------------------------- |
| 性质 | **报告存档**——某次测试做了什么、发现了什么、修没修 | **活文档**——测试套件怎么跑、分几层、效力边界 |
| 时效 | 记录**当时**的事实，不随后续改动更新               | 随代码同步维护                               |
| 读者 | 想知道「这个 bug 当初是怎么发现的」                | 想知道「现在怎么跑测试」                     |

**报告里的结论可能已被后续提交修正**，每份报告头部都标了状态。要了解当前行为，以代码与 `test/README.md` 为准。

> ⚠️ **行号会漂移。** 报告里的 `文件:行号` 是**写下当时**的位置，后续提交会让它们失准。凡标「修复前」的是**历史陈述**，对不上是正常的；其余引用若对不上，以符号名（函数名、字段名）为准去搜，别按行号读。
>
> ⚠️ **文件名也变过。** CLI 已于 **2026-09-20** 由 `scripts/purple-star.mjs` 迁移为
> `scripts/purple-star.ts`（行为不变）。本目录下 01 / 03 / 04 / 05 四份报告写于迁移之前，
> 其中的 `purple-star.mjs` 路径与行号**指向的是迁移前的那个文件**，现仓库里已不存在。
> 按存档惯例这些引用**刻意不改** —— 它们是当时的证据；要读现在的代码，认 `purple-star.ts`。
>
> ⚠️ **文件后来又拆过。** 同日 CLI 再按职责拆进 `scripts/cli/`（args / birth-info / render /
> commands / selftest），`purple-star.ts` 此后只剩引导层。报告里若提到 CLI 的某个函数
> （如 `calcTrueSolar`、`renderPalace`），请到 `scripts/cli/` 下按**函数名**去找——
> 这正是上一条「以符号名去搜」的用处。此项同样**只在此处备案，不回改报告**。

---

## 报告清单

| #   | 报告                                                | 对象                                | 核心结论                                                                                                                                                                                            | 状态                               |
| --- | --------------------------------------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| 01  | [skill 结构测试](01-skill-structure.md)             | SKILL.md / CLI / 文档一致性         | 发现 P1–P8；P1（性别护栏缺失）会**直接导致错误交付**                                                                                                                                                | ✅ P1–P5 已修；P8 未处理           |
| 02  | [内核虚岁口径](02-core-nominal-age.md)              | `algorithm.ts` 的 `currentAge`      | `currentAge` 用了周岁而大限区间是虚岁，**实测 1872/1872 全错**                                                                                                                                      | ✅ 已修 + 独立预言机验证           |
| 03  | [真太阳时口径](03-true-solar-time.md)               | `calcTrueSolar` / `--eot`           | 均时差是否计入是**口径选择**，做成 `--eot` 显式开关                                                                                                                                                 | ✅ 已实现；显示/判定缝隙**悬置**   |
| 04  | [交叉验证 · taibu](04-crossval-taibu.md)            | 本项目 vs `reference/taibu`         | 排盘层**逐字段相同**；但挖出「真太阳时跨午夜不回退日期」的 bug                                                                                                                                      | ✅ **已修**，复验 126/126 分歧归零 |
| 05  | [数据集核验与盲区补测](05-corpus-and-blindspots.md) | 全量语料 / 格局 / 合盘 / 文档一致性 | 格局与合盘零基准 → **自建安星规律等价关系**；文档 6 处漂移**已修**；P1 的处置方向**已被反转**（宫名改回项目本位）；后补**70 个格局名的层 3 独立预言机**，并抓出「杀破狼」`palaces` 为空（64% 命中） | 🔄 核验进行中；✅ 其余已完成       |

---

## 当前测试基线

改动内核或升级 `iztro` 后必须两层都跑。以下是最近一次全绿时的数字：

| 层         | 命令                                    | 结果          |
| ---------- | --------------------------------------- | ------------- |
| CLI 自检   | `node scripts/purple-star.ts selftest` | **44 / 44**   |
| CLI 端到端 | `node --test test/cli.test.mjs`         | **22 / 22**   |
| 基准回归   | `npm test`                              | **406 / 406** |

> 断言数变动史：`selftest` 33 → 37（P1 性别护栏）→ 40（`--eot`）→ 44（日期跨天回退）；`npm test` 341 → 342（虚岁修复 + 预言机）→ 343（`--eot`）→ 346（跨天回退的端到端断言）→ 351（宫名口径的 5 条层 3 预言机）→ 356（`--focus` 与 `heming` 的 5 条层 2 断言）→ **406**（格局识别的 50 条层 3 独立预言机）。

---

## 贯穿五份报告的一条教训

02、04、05 挖出的是**同一类问题**，值得单独点出来：

> **比对器的期望值必须来自另一条计算路径，否则它就只是内核的复读机。**

02 里 `test/lib/compare.mjs` 的 `expectedAge()` 与内核 `algorithm.ts` 的 `currentAge` **是同一个公式**，内核算错、比对器跟着错，300 条基准逐条"全等"。修复后加的那条反向实验最能说明问题：把内核与比对器**一起**改回错的写法，342 项里 **341 项照旧全绿，只有独立预言机断言变红**。

04 里 taibu 的排盘层与本项目**同源 iztro**，`bySolar(...)` 调用逐字相同——所以那一层能验证的只是「各自的集成层」，不是「排得对不对」。真太阳时均时差公式两边也**逐字相同**，同样不能互证。

05 里宫名归一化表（比对器翻译 baseline 用的）**就是从内核 import 的同一张** `IZTRO_TO_PROJECT_PALACE`。把它故意改错（`仆役 → 官禄宫`），300 条基准**照旧全绿**，只有层 3 那两条**不读该表**的预言机变红 —— 同一形状的第三次实证。

**判断一个交叉验证有没有价值，先问：它和被测对象共享了多少计算路径。**

---

## 未决事项

| 事项                    | 出处                              | 说明                                           |
| ----------------------- | --------------------------------- | ---------------------------------------------- |
| 显示值 vs 判定值的缝隙  | [03](03-true-solar-time.md)       | 交界 ±0.5 分内，用户按显示值算会得相反结论     |
| `reference/` 措辞的张力 | [01](01-skill-structure.md)       | P8，风险低，未处理                             |
| 虚岁分界无古籍佐证      | [02](02-core-nominal-age.md)      | 工程上对齐 iztro 即可，属知识空白              |
| 内核时区行为未实测      | [02](02-core-nominal-age.md)      | 用进程本地 `new Date()`，TZ 不一致会怎样无实验 |
| 火贪/铃贪的煞星口径     | [05](05-corpus-and-blindspots.md) | 32 次命中里 12 次煞星照不到命宫；收紧与否待定  |
